#include <stdio.h>

int main() {
    for (int row = 8; row >= 1; row--) {

        for (char col = 'A'; col <= 'H'; col++) {
            printf("%c%d ", col, row);
        }
        printf("\n");
    }
    
    return 0;
}
int main()
{
    int Zeile = 8;
    int Reihe = 8;
    for(Zeile )
}